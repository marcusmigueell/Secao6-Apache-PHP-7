<!DOCTYPE html>

<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <title>Curso PHP</title>
</head>

<body>

    <?php

        /*
            Operadores de comparação

            Igual (==) = verifica se os valores comparados são iguais
            Identico (===) = verifica se os valores comparados são iguais e do mesmo tipo
            Diferente (!=) = verifica se os valores comparados são diferentes
            Diferente (<>) = verifica se os valores comparados são diferentes
            Não Identico (!==) = verifica se os valores comparados são diferentes e de tipos diferentes
            Menor (<) = verifica se o valor da esquerda é menor que o valor da direita
            Maior (>) = verifica se o valor da esquerda é maior que o valor da direita
            Menor igual (<=) = verifica se o valor da esquerda é menor ou igual que o valor da direita
            Maior igual (>=) = verifica se o valor da esquerda é maior ou igual que o valor da direita

                if(5 === '5'){
                    //será executado se a condição for true
                } else {
                    //será executado se a condição for false
                }

            -------------------------------------------------------------------------------------------------------

            Operadores Lógicos

            Operador E (AND ou &&) - Verdadeiro se todas as expressões forem verdadeiras
            Operador OU (OR ou ||) - Verdadeiro se pelo menos uma das expressões for verdadeira
            Operador Xor (XOR) - Verdadeiro apenas se uma das expressões for verdadeira, mas não ambas
            Operador E (!) - Inverte o resultado da expressão

                if(expressao1 || expressao2){
                    //será executado se a condição for true
                } else {
                    //será executado se a condição for false
                }

            -------------------------------------------------------------------------------------------------------

                if(condicao) { //a condição retornará true ou false
                    //será executado se a condição for true
                } else {
                    //será executado se a condição for false
                }

                if(condicao1) { //a condição retornará true ou false
                    //será executado se a condição for true
                } else if(condicao2){
                    //será executado se a condicao1 for false e condição2 for true
                } else {
                    //será executado se a condicao1 for false e condição2 for false
                }

            -------------------------------------------------------------------------------------------------------

                Operador Ternário

                condicao ? // será retornado se true : // será retornado se false

        */

    ?>

    
    
</body>

</html>